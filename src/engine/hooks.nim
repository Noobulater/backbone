#type hookEntry = tuple[ name: string, hook: proc() ]

#var registeredHooks: seq[ref hookEntry] = @[] #sequence of tuples

#proc register( name: string, func: proc() ) =

#  registeredHooks.add( hookEntry( ) )
